import { Button } from '../../components/Button';

export default function LegalCredentialsPage() {
  return (
    <div className="flex min-h-screen items-center justify-center p-4">
      <div className="w-full max-w-md space-y-4 text-center">
        <h1 className="text-xl font-semibold">Step 3 of 4 – Legal &amp; Credentials</h1>
        <p className="text-gray-700">This step will collect your insurance and licensing details.</p>
        <Button onClick={() => {}} disabled className="w-full">Coming Soon</Button>
      </div>
    </div>
  );
}
